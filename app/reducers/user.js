'use-strict';
const initialState = {
  user: {
    username: 'yume',
    id: '1'
  }
};

export function user(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
